# Windows scripts must keep CRLF so cmd.exe and PowerShell 5.1 never see bare LF
*.ps1 text eol=crlf
*.bat text eol=crlf
# Unix scripts must keep LF or the shebang breaks
*.sh  text eol=lf
*.md    text
*.yml   text eol=lf
LICENSE text
